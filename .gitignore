# OS
.DS_Store
Thumbs.db

# Editors
*.swp
*~
.idea/
.vscode/

# Node
node_modules/
npm-debug.log*

# Logs
*.log

# Headless-driver output (tools/drive.mjs screenshots etc.)
tools/out/

# Local tool state (keep .claude/launch.json tracked)
.claude/settings.local.json
.claude/*.local.json

# Local deployment settings (make init) — billing/project ids stay local
deploy/config.mk

# GET /js/tunables.js is generated from dice.yaml by server.js, in memory
# (docs/DEVMODE.md). It is never on disk; a stray build of it must not commit.
js/tunables.js
armed-suite-main.js.bak
shots/
